import { Box } from '@mui/material';
const TitleComponent = () => {
    return (
        <Box
            style = {{
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center'
        }}>
        "Title"
    </Box>);
        };

        export default TitleComponent;